buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {

    }
}

tasks.register('clean', Delete) {
    delete new File(getRootDir(), "out")
    delete new File(getRootDir(), "build")
}